# ywsj-kms 环境变量配置
# 复制此文件为 .env 并修改后使用: cp .env.example .env

# Web面板登录凭据 (仅首次启动创建账号时使用，之后通过Web界面修改)
ADMIN_USER=admin
ADMIN_PASS=changeme_to_strong_password

# 受信任代理跳数 (直连=0, 单层nginx反代=1)
TRUSTED_PROXY_HOPS=0

# API限流 (每分钟请求数)
API_RATE_LIMIT=60
